# Azure Communication Services Configuration
# Get these values from your Azure Communication Services resource in the Azure Portal

# Connection string from Azure Communication Services resource
# Go to Azure Portal > Your ACS Resource > Keys > Copy "Connection string"
AZURE_COMMUNICATION_CONNECTION_STRING=endpoint=https://<resource-name>.communication.azure.com/;accesskey=<access-key>

# Phone number acquired from Azure Communication Services (for making/receiving calls)
# Go to Azure Portal > Your ACS Resource > Phone numbers > Get a phone number
# Format: +1234567890
AZURE_PHONE_NUMBER=

# Callback URL for receiving call events (webhook)
# For local development: http://localhost:5000
# For remote deployment: https://your-domain.com (IMPORTANT: use HTTPS!)
CALLBACK_BASE_URL=https://your-domain.com

# Flask configuration
# Generate a secure random key: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE-THIS-TO-A-RANDOM-SECRET-KEY

# Set to False for production, True for local development
DEBUG=False
